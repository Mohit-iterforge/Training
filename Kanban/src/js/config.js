/**
 * Base URL for the json-server API.
 * @type {string}
 */
export const API_BASE_URL = 'http://localhost:3000';

/**
 * Storage key used to persist the currently logged-in user.
 * @type {string}
 */
export const AUTH_STORAGE_KEY = 'kanbanAuthenticatedUser';